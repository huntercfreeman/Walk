<div class="di_ide_delete-file-form-display"
     @onkeydown="HandleOnKeyDown">

    <div>
        @* The text is: "Delete" or "Delete Recursively" *@
        <em class="di_em">
            Delete
            @if (AbsolutePath.IsDirectory)
            {
                <text>&nbsp;Recursively</text>
            }
        </em>

        :
        &nbsp;

        <em class="di_em" title="@AbsolutePath.Value">
            @AbsolutePath.NameWithExtension
        </em>

        @if (AbsolutePath.IsDirectory)
        {
            <div>
                AND
                <em class="di_em">
                    AT LEAST
                    @_countOfImmediateChildren
                </em>
                child files.
            </div>
        }
    </div>

    <div style="display: flex; justify-content: space-between;">
        <button class="di_button"
                @onclick="DeleteFileOnClick">
            Delete
        </button>

        <button class="di_button"
                @ref="_cancelButtonElementReference"
                @onclick="CancelOnClick">
            Cancel
        </button>
    </div>
</div>