@{
    var localDiffResult = DiffResult;
}

<div class="di_diff-details-visualization-in">
    @for (var i = 0; i < localDiffResult.InText.Length; i++)
    {
        var index = i;
        var character = localDiffResult.InText[index];

        <div class="di_diff-details-visualization-character"
             style="@($"top: {index * 2}em; left: 0;")">
            @character
        </div>
    }
</div>