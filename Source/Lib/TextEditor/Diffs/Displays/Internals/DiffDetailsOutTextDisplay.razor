@{
    var localDiffResult = DiffResult;
}

<div class="di_diff-details-visualization-out">
    @for (var i = 0; i < localDiffResult.OutText.Length; i++)
    {
        var index = i;
        var character = localDiffResult.OutText[index];

        <div class="di_diff-details-visualization-character"
             style="@($"left: {index * 2}em; top: 0;")">
            @character
        </div>
    }
</div>