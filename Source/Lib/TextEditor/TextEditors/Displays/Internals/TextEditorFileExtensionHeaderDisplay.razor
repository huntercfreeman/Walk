@{ var headerType = TextEditorService.GetHeader(_fileExtensionCurrent); }

@if (headerType is not null)
{
	<DynamicComponent Type="TextEditorService.GetHeader(_fileExtensionCurrent)"
				  	Parameters="_componentInnerParameters"/>
}
else
{
	<TextEditorDefaultHeaderDisplay ComponentDataKey="ComponentDataKey"/>
}
