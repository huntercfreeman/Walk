namespace Walk.Common.RazorLib.Badges.Models;

/// <summary>
/// It isn't correct to have this enum be so specific as, 'DirtyResourceUri', and 'Notification'.
/// But, the final solution still generally follows this same path, so this is progress.
/// </summary>
public enum BadgeKind
{
	DirtyResourceUri,
	Notification,
}
